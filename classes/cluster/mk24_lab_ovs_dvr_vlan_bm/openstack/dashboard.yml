classes:
- system.linux.system.repo.tcp_mk20
- system.horizon.server.single
- cluster.mk24_lab_ovs_dvr_vlan_bm
parameters:
  horizon:
    server:
      version: mitaka
  linux:
    system:
      repo:
        tcpcloud_openstack:
          source: deb [arch=amd64] http://apt.tcpcloud.eu/nightly/ trusty mitaka
    network:
      interface:
        eth1:
          enabled: true
          type: eth
          proto: dhcp
