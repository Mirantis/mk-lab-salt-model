classes:
- cluster.mk22_scale_mirantis
parameters:
  linux:
    network:
      interface:
        eth1:
          enabled: true
          type: eth
          proto: static
          address: ${_param:single_address}
          netmask: 255.255.255.0
