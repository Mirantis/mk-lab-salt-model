parameters:
  _param:
    gerrit_admin_user: admin
  jenkins:
    client:
      credential:
        gerrit:
          username: ${_param:gerrit_admin_user}
          key: ${_param:gerrit_admin_private_key}
