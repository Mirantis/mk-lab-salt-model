classes:
- service.cinder.control.single
parameters:
  cinder:
    volume:
      enabled: false
    controller:
      backend: {}
      default_volume_type: ''
      database:
        host: ${_param:single_address}

