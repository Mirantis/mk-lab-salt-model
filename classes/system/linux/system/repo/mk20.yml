parameters:
  linux:
    system:
      repo:
        tcpcloud_openstack:
          source: "deb [arch=amd64] http://apt.tcpcloud.eu/nightly/ trusty ${_param:openstack_version}"
          architectures: amd64
          key_url: "http://apt.tcpcloud.eu/public.gpg"