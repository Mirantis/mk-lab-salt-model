parameters:
  horizon:
    server:
      plugin:
        contrail:
          source:
            engine: pkg
            name: openstack-dashboard-contrail-panels
        horizon_overrides:
          overrides:
          - contrail_openstack_dashboard
