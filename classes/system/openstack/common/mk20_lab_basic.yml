classes:
- system.openstack.common.mk20_lab
parameters:
  linux:
    network:
      host:
        ctl01:
          address: ${_param:cluster_node01_address}
          names:
          - ${_param:cluster_node01_hostname}
          - ${_param:cluster_node01_hostname}.mk20-lab-basic.local
        ctl02:
          address: ${_param:cluster_node02_address}
          names:
          - ${_param:cluster_node02_hostname}
          - ${_param:cluster_node02_hostname}.mk20-lab-basic.local
        ctl03:
          address: ${_param:cluster_node03_address}
          names:
          - ${_param:cluster_node03_hostname}
          - ${_param:cluster_node03_hostname}.mk20-lab-basic.local
        cmp01:
          address: 172.16.10.105
          names:
          - cmp01
          - cmp01.mk20-lab-basic.local
