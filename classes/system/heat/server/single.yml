classes:
- service.heat.server.single
parameters:
  heat:
    server:
      stack_domain_admin:
        name: heat_domain_admin
        password: ${_param:heat_domain_admin_password}
        domain: heat
