parameters:
  reclass:
    storage:
      node:
        prx01:
          name: prx01
          domain: ${_param:cluster_domain}
          classes:
          - cluster.${_param:cluster_name}.openstack_dashboard
          params:
            salt_master_host: ${_param:reclass_config_master}
            linux_system_codename: trusty
            single_address: 172.16.10.121
