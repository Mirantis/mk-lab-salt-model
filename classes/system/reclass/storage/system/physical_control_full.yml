parameters:
  reclass:
    storage:
      node:
        kvm01:
          name: kvm01
          domain: ${_param:cluster_domain}
          classes:
          - cluster.${_param:cluster_name}.fuel.compute
          params:
            salt_master_host: ${_param:reclass_config_master}
            linux_system_codename: trusty
            single_address: ${_param:kvm01_single_address}
        kvm02:
          name: kvm02
          domain: ${_param:cluster_domain}
          classes:
          - cluster.${_param:cluster_name}.fuel.compute
          params:
            salt_master_host: ${_param:reclass_config_master}
            linux_system_codename: trusty
            single_address: ${_param:kvm02_single_address}
        kvm03:
          name: kvm03
          domain: ${_param:cluster_domain}
          classes:
          - cluster.${_param:cluster_name}.fuel.compute
          params:
            salt_master_host: ${_param:reclass_config_master}
            linux_system_codename: trusty
            single_address: ${_param:kvm03_single_address}
        kvm04:
          name: kvm04
          domain: ${_param:cluster_domain}
          classes:
          - cluster.${_param:cluster_name}.fuel.compute
          params:
            salt_master_host: ${_param:reclass_config_master}
            linux_system_codename: trusty
            single_address: ${_param:kvm04_single_address}
        kvm05:
          name: kvm05
          domain: ${_param:cluster_domain}
          classes:
          - cluster.${_param:cluster_name}.fuel.compute
          params:
            salt_master_host: ${_param:reclass_config_master}
            linux_system_codename: trusty
            single_address: ${_param:kvm05_single_address}
        kvm06:
          name: kvm06
          domain: ${_param:cluster_domain}
          classes:
          - cluster.${_param:cluster_name}.fuel.compute
          params:
            salt_master_host: ${_param:reclass_config_master}
            linux_system_codename: trusty
            single_address: ${_param:kvm06_single_address}
        kvm07:
          name: kvm07
          domain: ${_param:cluster_domain}
          classes:
          - cluster.${_param:cluster_name}.fuel.compute
          params:
            salt_master_host: ${_param:reclass_config_master}
            linux_system_codename: trusty
            single_address: ${_param:kvm07_single_address}
        kvm08:
          name: kvm08
          domain: ${_param:cluster_domain}
          classes:
          - cluster.${_param:cluster_name}.fuel.compute
          params:
            salt_master_host: ${_param:reclass_config_master}
            linux_system_codename: trusty
            single_address: ${_param:kvm08_single_address}
        kvm09:
          name: kvm09
          domain: ${_param:cluster_domain}
          classes:
          - cluster.${_param:cluster_name}.fuel.compute
          params:
            salt_master_host: ${_param:reclass_config_master}
            linux_system_codename: trusty
            single_address: ${_param:kvm09_single_address}
