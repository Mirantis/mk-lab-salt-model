parameters:
  glusterfs:
    client:
      volumes:
        glance:
          path: /var/lib/glance/images
          server: ${_param:glusterfs_service_host}
          user: glance
          group: glance
